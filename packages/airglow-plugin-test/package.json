{
  "name": "@airglow/plugin-test",
  "version": "0.4.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/adobe/airglow.git",
    "directory": "packages/airglow-plugin-test"
  },
  "publishConfig": {
    "access": "public"
  },
  "description": "A plugin that stores dispatches for testing",
  "author": "Adobe Systems",
  "license": "Apache",
  "files": [
    "dist/"
  ],
  "main": "dist/index.js",
  "dependencies": {
    "@airglow/reducers": "^0.4.0",
    "airglow": "^0.4.0"
  },
  "devDependencies": {
    "@airglow/store-react": "^0.4.0",
    "@babel/cli": "^7.4.4"
  },
  "scripts": {
    "prebuild": "rm -rf dist && mkdir dist",
    "build": "babel src --out-dir dist --root-mode upward",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
