{
  "name": "@airglow/store-redux",
  "version": "0.0.1",
  "description": "A react module loader",
  "author": "Adobe Systems",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "dependencies": {
    "airglow": "*",
    "@airglow/reducers": "*",
    "ramda": "^0.26.1",
    "react": "^16.8.4",
    "react-redux": "^7.0.3",
    "redux": "^4.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1"
  },
  "scripts": {
    "prebuild": "rm -rf dist && mkdir dist",
    "build": "babel src --out-dir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
