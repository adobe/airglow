{
  "name": "airglow",
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/adobe/airglow.git",
    "directory": "packages/airglow"
  },
  "description": "Simple Clean React Framework",
  "author": "Adobe Systems",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "dependencies": {
    "ramda": "^0.26.1",
    "react": "^16.8.4"
  },
  "devDependencies": {
    "@babel/cli": "^7.4.0"
  },
  "scripts": {
    "prebuild": "rm -rf dist && mkdir dist",
    "build": "babel src --out-dir dist --root-mode upward",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
